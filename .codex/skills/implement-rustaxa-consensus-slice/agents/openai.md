interface:
  display_name: "Implement Rustaxa Consensus Slice"
  short_description: "Advance a Rustaxa consensus rewrite slice."
  default_prompt: "Select the next ready CRW item from the consensus tracker and implement one bounded Rustaxa consensus rewrite slice. Require code-mapper research and the independent reviewer approval loop; use API, architecture, Rust, and C++ agents when their boundaries are in scope, keep the tracker, consolidation plan, and bridge audit synchronized, and apply the documented fallback-emulation policy when native named-agent invocation is unavailable."
