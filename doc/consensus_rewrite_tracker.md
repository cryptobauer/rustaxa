# Consensus Rewrite Tracker

This tracker expands the consensus section in `PLAN.md`. Keep it current as consensus code moves from C++ into Rust.
Required test coverage and parity gates for the Rust consensus model are defined in
`doc/consensus_model_test_plan.md`.

## Status Legend

| Status | Meaning |
| --- | --- |
| `not-started` | No Rust port exists yet. |
| `rust-domain` | Pure Rust domain model or deterministic algorithm exists without C++ runtime wiring. |
| `rust-backed` | C++ public API is backed by Rust for this behavior. |
| `partial` | Some Rust-backed behavior exists, but documented gaps remain before the API can replace C++ semantics. |
| `shim-stubbed` | Rust mode has an explicit temporary default/no-op. Must not delegate to legacy C++. |
| `cpp-owned` | Intentionally left in C++ for now. |
| `deferred` | Out of current consensus rewrite scope. |

## Hard Rules

- Do not delegate Rust shim behavior back to legacy C++ implementation methods.
- Keep public C++ APIs stable unless a task explicitly changes the API.
- Main-only files referenced from upstream-owned C++ files must be included only behind `RUSTAXA_ENABLE=1` or a narrower Rustaxa feature guard.
- Treat DPoS eligibility and vote-count behavior as required consensus functionality. Temporary defaults must stay visible in this tracker.
- Keep network callbacks, daemon threads, peer gossip, and full-node orchestration in C++ until the Rust domain services are stable.
- New consensus rewrite APIs must be shaped for the upcoming application-owned arena/data pipeline even before the
  concrete pipeline API lands. Prefer canonical bytes, compact facts, ingress-payload-addressable enrichment, and typed
  protocol plans/effects over eager C++ object materialization.

## Planned Arena/Data Pipeline Direction

The parallel Rust network rewrite will land an application-owned arena pipeline before this consensus feature branch is
merged. The first ingress point is expected to accept latest-tarcap payload bytes from
`TaraxaCapability::interpretCapabilityPacket`, write them into an application-global ingress arena, and report only
whether ingestion succeeded. Network-level outcomes such as drop, disconnect, mark-malicious, gossip, or sync request
will be emitted later by pipeline stages as egress events; they are not part of the ingress success contract.

The planned initial CXX bridge entry point is:

```rust
pub fn ingest_network_packet(
    self: &mut BridgeNetwork,
    packet_type: u8,
    from_node: [u8; 64],
    data: Vec<u8>,
) -> Result<bool>;
```

This API is latest-tarcap-only. Its `bool` reports ingestion success only: `true` means the payload bytes were accepted
into the application arena/pipeline. Broken payload, protocol rejection, consensus rejection, peer action, gossip, sync,
drop, and disconnect outcomes are produced later by downstream pipeline stages and network egress events.

Consensus rewrite slices should assume this future direction:

- Pipeline communication is by payload reference id. A stage may wrap the ingress payload reference id with small
  route/source/priority metadata, but large derived data should live in ingress or enrichment arenas and be referenced by
  payload reference id.
- Each ingress event is handled by only one thread at a time. Pipeline stages should model ownership transfer of an event
  carrying a payload reference, not fanout over shared mutable ingress state.
- The intended stage sequence is network ingress -> prefilter -> dispatcher -> pipeline-specific ring buffers -> effect
  executors. `NetworkEvent`, prefilter decisions, dispatcher classification, and ring-buffer allocation belong in the
  network crate or a dedicated pipeline crate. The consensus crate should define only consensus event/effect types and
  opaque ingress payload references. Dispatchers outside consensus route accepted ingress events into typed consensus
  units, such as `PbftVoteEvent`, before those units enter the owning pipeline's ring buffer. These type names, variants,
  and payload shapes are provisional and should evolve as the first routed pipeline validates the design.
- Materialization should be delayed. Ingress-adjacent Rust code should inspect raw bytes, produce compact facts or
  enrichment records only when useful, and avoid constructing C++ `PbftVote`, `PbftBlock`, `DagBlock`, `PeriodData`, or
  `Transaction` objects unless a temporary compatibility executor requires them.
- Consensus domain identity remains hash/period/round/step/voter/level based. Ingress payload references are data-plane
  handles and should not become consensus identities.
- Intermediate modules between network ingress and consensus, such as prefilters or route classifiers, are expected.
  Consensus-facing APIs should be callable from those stages with raw bytes, ingress-payload-backed views, or compact
  facts instead of requiring network handler objects.

The arena-backed data plane should expose multiple logical pipelines rather than one monolithic consensus pipeline.
Today's C++ scheduler has three priority lanes, but consensus behavior is better represented as seven logical pipelines:

| Logical pipeline | Current ingress message kinds | Cross-pipeline effects to keep explicit |
| --- | --- | --- |
| Peer status and sync control | `StatusPacket` | Peer readiness, peer PBFT/DAG progress, PBFT sync start, DAG sync request, next-vote request |
| Transaction gossip and admission | `TransactionPacket` | Peer-known transaction marks, transaction admission, peer-order blocking for later DAG blocks from the same peer |
| DAG block gossip and admission | `DagBlockPacket` | DAG verification/admission, missing transaction or tip responses, DAG sync request, PBFT-sync-aware DAG acceptance |
| DAG sync | `GetDagSyncPacket`, `DagSyncPacket` | Missing DAG/transaction recovery, peer DAG synced state, possible PBFT sync restart on period mismatch |
| PBFT vote and round progress | `VotePacket`, `VotesBundlePacket`, `GetNextVotesBundlePacket` | Vote admission, known-vote marks, proposed-block sidecar admission, slashing proof submission, PBFT round/finalization progress, gossip |
| PBFT chain sync and finalized-period intake | `GetPbftSyncPacket`, `PbftSyncPacket`, `PbftBlocksBundlePacket` | Deep-sync traffic filtering, sync-peer validation, period-data queue admission, malicious-sync-peer reporting, PBFT sync completion, DAG sync request |
| Pillar vote and bundle handling | `PillarVotePacket`, `GetPillarVotesBundlePacket`, `PillarVotesBundlePacket` | Pillar-vote admission, known-vote marks, pillar block/vote facts used by PBFT period validation |

Future Rust ingress-adjacent APIs should keep these pipeline facts and effects separate. Cross-pipeline impact should be
represented as typed effects, such as request-sync, block-peer-order, mark-known, admit, gossip, report-malicious,
enqueue-period-data, or drive-PBFT-progress, rather than hidden mutation of another pipeline's state.

## Consensus Protocol Planner Model

Consensus business logic should be expressed as deterministic protocol planners over explicit state views. A planner
receives a consensus event or command, compact facts, config/time inputs, and borrowed state views, then returns a
protocol plan. The plan describes the protocol state transition implied by the input: validation outcome data, ordered
state/write intents, follow-up consensus events, and external effects.

The planner itself must stay side-effect-free. It should not own long-lived consensus data, write storage, send network
messages, spawn Tokio tasks, block on async I/O, or mutate another pipeline directly. Runtime workers, actors,
ring-buffer stages, Tokio tasks, and effect executors may schedule planners and apply returned plans at the boundary, but
consensus rules should not be hidden inside mailbox-local actor state or async task choreography.

The term "transition" means the consensus system's protocol state is transitioning, not that each input produces only a
small transformation. A single PBFT vote event can legitimately plan known-vote marking, vote admission, proposed-block
sidecar admission, threshold updates, slashing proof emission, gossip, and PBFT progress triggers as one protocol plan
when those are the deterministic consequences of the event and current state view.

## Current Rust Starting Point

| Area | Rust location | Status | Notes |
| --- | --- | --- | --- |
| FinalChain read/index helper | `rust/crates/rustaxa-consensus/src/final_chain.rs` | `rust-backed` for selected FinalChain reads | Exists because FinalChain work started before consensus. It is not a PBFT/DAG port yet. |
| Consensus crate root | `rust/crates/rustaxa-consensus/src/lib.rs` | `rust-backed` for DAG graph | Exports the DAG graph model used by the C++ `Dag` wrapper and selected FinalChain read helpers. |
| Consensus event pipeline scaffold | `rust/crates/rustaxa-consensus/src/consensus_pipeline.rs` | scaffold | Defines only consensus-layer pipeline vocabulary: logical pipeline kinds, opaque ingress payload references, event origins, typed consensus events, the first `PbftVoteEvent`, typed cross-pipeline effects, and the provisional `ConsensusPlan` envelope returned by side-effect-free protocol planners. These names, variants, and payload fields are provisional and open to change until production routing proves the boundaries. `NetworkEvent`, prefilter decisions, dispatcher classification, and ring-buffer allocation are intentionally excluded and should live in the network crate or a dedicated pipeline crate. This scaffold does not decode payload bytes, mutate consensus state, or route production handlers yet. |
| PBFT vote ingress/progress planners | `rust/crates/rustaxa-consensus/src/pbft_vote_ingress.rs`, `rust/crates/rustaxa-consensus/src/pbft_vote_admission.rs`, `rust/crates/rustaxa-consensus/src/pbft_vote_event.rs`, `rust/crates/rustaxa-consensus/src/pbft_vote_progress.rs`, `rust/crates/rustaxa-consensus/src/pbft_vote_pipeline.rs`, `rust/crates/rustaxa-consensus/src/pbft_vote_runtime.rs`, `rust/crates/rustaxa-bridge/src/pbft_vote_ingress.rs`, `rust/crates/rustaxa-bridge/src/pbft_vote_admission.rs`, `rust/crates/rustaxa-bridge/src/pbft_vote_event.rs`, `rust/crates/rustaxa-bridge/src/pbft_vote_progress.rs`, `rust/crates/rustaxa-bridge/src/pbft_vote_pipeline.rs`, `rust/crates/rustaxa-bridge/src/verified_votes.rs` | `partial` | Side-effect-free protocol planners for PBFT vote ingress and vote pipeline decisions plus a Rust-owned admission runtime for Rust-mode `VoteManager::addVerifiedVote`. Rust now owns the deterministic single-vote and bundle ingress gates for relevance, period/round/step windows, proposed-vote bundle rejection, bundle identity consistency, and PBFT/next-vote sync hints. The production shim opens admission from canonical vote RLP plus explicit validation facts, so Rust carries canonical validation output, explicit replay mutation facts, and the validation-calculated weight into compact progress facts, mutates the single Rust-owned `VerifiedVotes` runtime, retains weighted storage payloads and unweighted slashing evidence payloads, owns PBFT `2t+1` threshold cache lookup/update, and returns one terminal executor report with typed peer-known, proposed-block sidecar, gossip, storage, slashing, threshold, and PBFT-progress intents. Retained weighted payload views now back legacy snapshot, reward-vote materialization, and 2t+1 materialization APIs, and missing retained payloads for Rust-owned selected votes are invariant errors instead of partial results. The Rust runtime also builds PBFT reward-vote candidate facts from its own verified-vote metadata and resolves selected reward records in PBFT-block requested order. The Rust-mode `VoteManager` shim now exposes the peer-known, gossip, slashing, and PBFT-progress portions as a shim-owned admission report; latest-tarcap single-vote and bundle handlers execute those effects only after Rust admission accepts the vote, and Rust-mode bundle rebroadcast includes only accepted votes. The older admission session, pipeline session, standalone progress planners, weight-supplied constructor, and low-level verified-vote mutators remain compatibility/test scaffolding rather than production state-transition authority. Durable reward persistence, slashing, progress, and current-round 2t+1 bundle persistence are exposed only after Rust has applied the verified-vote mutation. C++ still decodes network packets, supplies peer/live-sidecar facts, executes peer/network effects and proposed-block sidecar handling, and temporarily hosts guarded packet-handler ingress hooks until the network/tarcap pipeline overlay owns those routes. The latest-tarcap method signature changes are temporary hook debt, not the target pipeline API. |
| PBFT vote validation planner | `rust/crates/rustaxa-consensus/src/pbft_vote_validation.rs`, `rust/crates/rustaxa-consensus/src/pbft_thresholds.rs`, `rust/crates/rustaxa-bridge/src/pbft_vote_validation.rs`, `rust/crates/rustaxa-bridge/src/verified_votes.rs` | `partial` | Rust owns received-vote validation decisions, stable rejection statuses, replay-marker timing, canonical PBFT vote RLP inspection, signed/unsigned vote hash derivation, recovered voter identity, signature and VRF proof checks, Rust-computed received-vote weight, the PBFT sortition-threshold formula, runtime-owned replay protection, runtime-owned `2t+1` threshold lookup/current-period caching, and local proposer-sortition screening. The Rust-mode `VoteManager` shim routes `validateVote`, `addVerifiedVote`, `voteAlreadyValidated`, `getPbftTwoTPlusOne`, and `genAndValidateVrfSortition` through the `VerifiedVotes` runtime facade instead of `VoteManagerOld`; `addVerifiedVote` now collects FinalChain/key facts and lets Rust validation-backed admission produce the authoritative weight, verified-vote mutation, replay mutation, threshold decisions, and post-mutation executor effects. The older `BridgePbftVoteValidationRuntime` remains compatibility/test scaffolding, not production routing. C++ still supplies FinalChain DPoS facts, key-manager VRF-key facts, proposer sortition object materialization, and temporary live sidecar mutation. It only calls `PbftVote::calculateWeight` to hydrate the legacy sidecar after checking the hydrated sidecar weight matches Rust's calculated weight. |
| PBFT vote generation planner | `rust/crates/rustaxa-consensus/src/pbft_vote_generation.rs`, `rust/crates/rustaxa-bridge/src/pbft_vote_generation.rs` | `partial` | Rust owns side-effect-free local PBFT vote byte generation: it validates vote type/step compatibility, derives the expected voter/VRF public key from ephemeral wallet secrets, creates the legacy PBFT VRF proof/output, signs `PbftVote::sha3(false)`, returns canonical signed or weighted `PbftVote` RLP, and reports zero-stake, zero-total-DPoS, and zero-weight outcomes as stable statuses. The Rust-mode `VoteManager` shim now materializes `generateVote` and `generateVoteWithWeight` sidecars directly from Rust-generated RLP, hydrates the temporary C++ VRF output cache through local VRF verification, and checks Rust hashes, signing hash, recovered identity, VRF proof, weight, and exact RLP bytes before returning the sidecar. Locally generated own-vote persistence therefore stores Rust-generated weighted vote bytes through Rust storage. C++ still owns the temporary `PbftVote` sidecar type, FinalChain fact sourcing, logging, and PBFT manager/network orchestration. |
| PBFT vote payload builders | `rust/crates/rustaxa-consensus/src/pbft_vote_payload.rs`, `rust/crates/rustaxa-bridge/src/pbft_vote_payload.rs` | `partial` | Rust owns legacy-compatible PBFT vote payload construction for post-admission side effects. It derives weighted storage RLP records from canonical signed vote bytes plus the authoritative calculated weight, builds raw weighted vote-bundle RLP for latest-round 2t+1 persistence and finalized reward-vote reset stages, and normalizes unweighted signed vote RLP for slashing evidence so storage weights do not leak into slashing calldata. The Rust admission runtime now retains these payloads for accepted votes and returns storage/slashing-ready records to `VoteManager::addVerifiedVote`. C++ still selects live vote sidecars for remaining reload/finalization compatibility paths, supplies period/round/block metadata where Rust has not already returned it, executes Rust storage batches, and submits signed slashing transactions. |
| Shared DAG/PBFT types | `rust/crates/rustaxa-types/src/{dag.rs,pbft.rs}` and codec modules | partial | Useful for future consensus domain types, but not yet a full consensus model. |
| Storage ports | `rust/crates/rustaxa-storage/src/{dag.rs,pbft.rs,pillar.rs}` | partial infra | Use through narrow domain-facing ports; do not let consensus logic depend on broad storage APIs. |

## Module Inventory

| Module | Primary files | Approx size | Status | Proposed ownership | Notes |
| --- | --- | ---: | --- | --- | --- |
| DAG graph | `dag/dag.hpp`, `dag/dag.cpp`, `dag_shim/*` | 424 lines | `rust-backed` | Rust domain behind C++ overlay shim | C++ `Dag`/`PivotTree` graph operations route to Rust under `RUSTAXA_ENABLE` through a full overlay shim. Legacy Boost graph remains pure C++ fallback/reference code and is compiled as `DagOld`/`PivotTreeOld` only in Rust-enabled builds. |
| DAG manager | `dag/dag_manager.hpp`, `dag/dag_manager.cpp` | 1048 lines | partial | C++ shim plus Rust domain/infra ports | Manager orchestration remains C++; its `Dag`/`PivotTree` graph objects are Rust-backed in Rust mode. Depends on transaction manager, PBFT chain, storage, network, key manager, FinalChain. |
| DAG proposer | `dag/dag_block_proposer.hpp`, `dag/dag_block_proposer.cpp`, `dag_block_proposer_shim/*` | 576 lines | `partial` | C++ orchestration with Rust proposer policy | Full Rust-mode overlay shim landed. C++ still owns thread/network lifecycle, block construction, signing, and add-block wiring; transaction packing now enters the Rust-owned TransactionManager pack session, and Rust owns proposer eligibility status decisions, legacy VRF input bytes, and deterministic tip-selection policy. |
| Sortition params | `dag/sortition_params_manager.hpp`, `dag/sortition_params_manager.cpp` | 331 lines | `rust-backed` | Rust domain behind C++ overlay shim | Deterministic efficiency/threshold runtime state routes to `rustaxa-consensus::sortition` under `RUSTAXA_ENABLE_SORTITION_PARAMS`. PBFT finalization now uses a two-phase sortition contract: preview the Rust threshold transition without publishing live state, persist any emitted `SortitionParamsChange` inside the primary finalization batch, then commit the live Rust runtime only after storage succeeds and validate the emitted change matches the preview. C++ still owns storage reads/writes and batch atomicity; the legacy implementation is compiled as `SortitionParamsManagerOld` only for pure C++ reference builds. |
| PBFT chain | `pbft/pbft_chain.hpp`, `pbft/pbft_chain.cpp`, `pbft_chain_shim/*` | 259 lines | `rust-backed` | Rust domain behind C++ overlay shim | In-memory head updates, legacy JSON-head preview, and next-block validation route to Rust under `RUSTAXA_ENABLE_PBFT_CHAIN`. C++ shim preserves `DbStorage` lookup/persistence ownership and JsonCpp formatting; legacy implementation is compiled as `PbftChainOld` only for pure C++ reference builds. |
| Proposed blocks | `pbft/proposed_blocks.hpp`, `pbft/proposed_blocks.cpp`, `proposed_blocks_shim/*` | 178 lines | `rust-backed` | Rust domain behind C++ overlay shim | Proposed block membership, cached validation flags, RLP payload snapshots, old-block diagnostics, and cleanup planning route to Rust under `RUSTAXA_ENABLE_PROPOSED_BLOCKS`. C++ shim preserves `DbStorage` persistence/removal and `PbftBlock` materialization; legacy implementation is compiled as `ProposedBlocksOld` only for pure C++ reference builds. |
| Period data queue | `pbft/period_data_queue.hpp`, `pbft/period_data_queue.cpp`, `period_data_queue_shim/*` | 168 lines | `rust-backed` | Rust domain metadata behind C++ overlay shim | Admission rules, effective processable size, period tracking, pop vote-source decisions, cleanup planning, and clear semantics route to Rust under `RUSTAXA_ENABLE_PERIOD_DATA_QUEUE`. C++ keeps live `PeriodData`, `PbftVote`, and peer `NodeID` ownership. |
| PBFT manager | `pbft/pbft_manager.hpp`, `pbft/pbft_manager.cpp`, `pbft_manager_shim/*` | 3267 lines | `partial` | Full Rust-mode overlay shell plus Rust daemon-tick runtime | Highest complexity: state machine, finalization, gossip, threading, storage, DAG, votes, pillar, FinalChain. Rust-enabled builds now compile the upstream manager as `PbftManagerOld` and expose a shim-owned `PbftManager` overlay so original `pbft_manager.cpp` stays clean versus `upstream-main`. The overlay currently differs intentionally for startup proposed-block restore, Rust-backed pillar-vote sync validation, Rust-planned sync-period admission, and a Rust-owned daemon-tick action cursor. The copied manager body is accepted as PBFT orchestration scaffolding and should shrink as round/step/status planning moves into Rust. `rustaxa-consensus::pbft_manager` now owns the ordered control-flow script for one PBFT daemon tick: process synced blocks, optionally broadcast votes and try cert-block push while not syncing, try round advance, branch from a post-prestate eligible-wallet report, route ineligible-wallet sleep, run the current state action, select certify/finish-polling transitions from explicit C++ report flags, and request the final sleep. C++ remains the live executor for sleeps, network, block/vote object materialization, storage writes, proposal/vote generation, and state-transition helper calls; each action reports back before Rust advances the cursor. `processPeriodData` now sends PBFT chain linkage, FinalChain hash status, reward/cert vote status, transaction warning facts, pillar-data status, and pillar-vote status to a side-effect-free Rust planner that returns accept/drop/wait/clear-and-report intents while C++ keeps logging, waits, queue mutation, peer reporting, and live object dispatch. Missing and finalized transaction facts are still warn-only for compatibility. PBFT finalization now coordinates shim-owned live-action executors for sortition runtime commit, reward-vote metadata commit, DAG order, transaction finalized-status cleanup, and PBFT-chain head update; each executor returns a Rust-verifiable post-state report before the runtime cursor advances. The pillar-vote sync path inspects pillar-vote RLP/signatures in Rust, uses the Rust-recovered voter for C++ FinalChain DPoS weight lookup, calls the Rust pillar-vote bundle planner for deterministic period/block/prevalidation/unique-weight threshold decisions, and inserts only Rust-accepted vote hashes with Rust-planned weights and Rust-recovered voter identities. It no longer calls `PillarChainManager::validatePillarVote`, `addVerifiedPillarVote`, `PillarVote::verifyVote`, or `PillarVote::getVoterAddr` on this sync path. |
| Verified votes | `vote_manager/verified_votes.hpp`, `vote_manager/verified_votes.cpp`, `verified_votes_shim/*` | 384 lines | `rust-backed` | Rust domain behind C++ overlay shim | Unique-voter checks, voted-value weight aggregation, 2t+1 block mappings, period cleanup, and round t+1 markers route to `rustaxa-consensus::verified_votes` through `RUSTAXA_ENABLE_VERIFIED_VOTES`. The bridge handle now owns `PbftVoteAdmissionRuntime`, making verified-vote metadata and retained PBFT vote payload sidecars a single Rust runtime owner for `VoteManager::addVerifiedVote`. C++ shim owns live `PbftVote` objects only for temporary compatibility and reconstructs legacy snapshot types for callers; accepted live sidecars are attached after Rust has accepted and mutated the runtime. Generic snapshot reads, reward-vote selection/materialization, and 2t+1 voted-block vote reads now materialize temporary `PbftVote` sidecars from Rust-retained weighted payload bytes rather than skipping missing live sidecars; metadata-only compatibility/test helper inserts may still fall back to `live_votes_` until those helper surfaces are removed. Rust-owned selected reward votes and 2t+1 mappings require retained payloads for every selected vote hash. The Rust bridge still exposes combined verified-vote add/threshold and step-scope snapshot helpers for compatibility tests and future direct consumers. 2t+1 mapping insertion in Rust mode is first-writer-wins. Legacy implementation is compiled as `VerifiedVotesOld` for pure C++ reference builds. |
| Vote manager | `vote_manager/vote_manager.hpp`, `vote_manager.cpp`, `vote_manager_shim/*` | 1145 lines | `partial` | Rust domain for validation/aggregation/storage/generation; C++ network/live sidecar shell | The Rust-mode `VoteManager` overlay now routes deterministic verified-vote state methods through the Rust-backed `VerifiedVotes` facade: `addVerifiedVote`, vote presence/uniqueness, verified-vote snapshots and counts, period cleanup, proposal-vote selection, next-round detection, 2t+1 block/bundle lookup, step snapshots, current period/round bundle persistence, reward-vote selection/materialization, and network t+1 step reads. `addVerifiedVote` now enters a validation-backed Rust PBFT vote admission runtime: the shim collects FinalChain/key facts, Rust validates canonical PBFT vote RLP, returns explicit replay mutation facts and the authoritative calculated weight, derives compact progress facts, mutates the Rust-owned verified-vote index, applies threshold decisions through the same runtime handle, retains weighted storage payloads and unweighted slashing payloads, and returns one terminal executor report with Rust-owned peer-known, proposed-block sidecar, gossip, storage, slashing, threshold, and PBFT-progress intents. C++ validates those intents against the temporary live sidecar, executes storage/slashing/logging through existing boundaries, and leaves peer/network execution with current packet/PBFT-manager callers until the ingress pipeline moves. VoteManager PBFT vote persistence now routes through VoteManager-specific `rustaxa-storage` bridge operations and Rust vote payload builders: locally generated own votes write Rust-generated weighted RLP into `latest_round_own_votes` through Rust before mutating the live own-vote sidecar, accepted-vote progress persists Rust-returned optional extra reward votes and optional latest-round 2t+1 bundles through one Rust storage batch, finalized reward-vote reset stages receive Rust-built raw weighted vote-bundle RLP, and own-vote cleanup appends exact vote-hash deletes to the caller-owned Rust batch before clearing live sidecars. Startup reload still materializes C++ `PbftVote` sidecars from Rust-backed `DbStorage` reads. `validateVote`, `voteAlreadyValidated`, `getPbftTwoTPlusOne`, and `genAndValidateVrfSortition` no longer forward to `VoteManagerOld`: Rust owns validation/replay planning, canonical received-vote RLP inspection, recovered voter identity, signature and VRF proof checks, Rust-computed received-vote weight, replay-cache storage, sortition-threshold calculation, Rust-owned threshold lookup/current-period caching, and local proposer-sortition screening through the `VerifiedVotes` runtime facade. `generateVote` and `generateVoteWithWeight` now call Rust signed/weighted vote generation once and directly materialize the returned sidecar from Rust-generated RLP; the shim verifies local VRF output to hydrate the temporary C++ credential cache needed by PBFT proposer selection. `checkRewardVotes` now calls the Rust verified-vote runtime to build preferred-round and reverse-period reward candidates from Rust metadata, evaluate the PBFT block's requested hashes, and return selected retained weighted records in requested order; C++ only materializes those records when copied votes are requested. Verified-vote snapshots, reward-vote reads, and 2t+1 bundle reads no longer skip missing live sidecars because the shim materializes from Rust-retained weighted payloads. C++ remains the executor for FinalChain/key-manager fact sourcing, live `PbftVote` sidecar compatibility, slashing transaction construction/submission from Rust-normalized payloads, logging, reward-vote sidecar mapping, and current network effects. PBFT finalization reward-vote reset now sources the cert `2t+1` vote set through the Rust payload-backed verified-vote API before building the Rust storage stage, and its post-commit metadata update returns a Rust-validated live-action proof for period, round, block hash, and stale extra-vote cleanup. Depends on FinalChain DPoS, VRF, slashing, storage, network. |
| Pillar chain manager | `pillar_chain/pillar_chain_manager.hpp`, `pillar_chain_manager.cpp`, `pillar_chain_manager_shim/*` | 427 lines | `partial` | Full Rust-mode overlay shell with Rust pillar planning | Rust-enabled builds now compile the upstream manager as `PillarChainManagerOld` and expose a shim-owned manager overlay while original pillar-chain manager files stay clean versus `upstream-main`. Rust owns pillar-vote relevance, inspection, recovered-voter uniqueness, insertion into the Rust-backed `PillarVotes` index, sync-bundle planned insertion, validator vote-count delta planning for pillar blocks, and pillar-block parent/period linkage planning. C++ still owns FinalChain DPoS fact sourcing, bridge root/epoch sourcing, `PillarBlock`/`PillarVote` object materialization, signing, storage writes, live vote sidecars, event emission, network requests, and finalization orchestration. |
| Transaction queue | `transaction/transaction_queue.hpp`, `transaction/transaction_queue.cpp`, `transaction_queue_shim/*` | 501 lines | `rust-backed` | Rust metadata and payload domain behind C++ overlay shim | Deterministic queue metadata, per-account nonce ordering, same-nonce replacement, non-proposer expiry planning, finalized-account purge planning, pool limits, gas-price threshold accounting, queued transaction RLP payload retention, known-cache expiration, and overflow/drop observation state route through Rust under `RUSTAXA_ENABLE_TRANSACTION_QUEUE`. C++ materializes `Transaction` objects from Rust-retained RLP; TransactionManager finalized-account purge and standalone `TransactionQueue::purge()` now source account nonce facts from Rust FinalChain. The obsolete fact-supplied queue purge bridge APIs have been removed from the CXX boundary. |
| Transaction manager | `transaction/transaction_manager.hpp`, `transaction/transaction_manager.cpp`, `transaction_manager_shim/*` | 837 lines | `partial` | Rust-owned live sidecars plus C++ materialization/orchestration shell | `packTrxs` candidate snapshotting, candidate scan, Rust-inspected candidate envelope facts for EVM input, declared-gas fit checks, invalid-estimate demotion mutation, accepted output ordering, accepted gas accounting, and stop decisions route through a Rust runtime pack session under `RUSTAXA_ENABLE`; C++ now uses a Rust step protocol that returns either the next EVM estimate request or final selected RLP outputs, and materializes only final accepted outputs. A shim-owned guard serializes pack calls around the single Rust runtime session while C++ performs EVM execution outside the transaction lock. The Rust-mode shim owns an opaque Rust runtime handle for authoritative transaction count, live queue metadata/payloads, known-cache state, live non-finalized/recently-finalized transaction sidecars, and bounded opaque `ExecutionResult` gas-estimation cache entries keyed by transaction hash plus proposal period. `estimateTransactionGas` and `estimateTransactions` now ask Rust for declared-gas shortcut, cache-hit, cache-miss, and cache-store decisions while C++ keeps EVM execution and result materialization. `isTransactionKnown` now routes through Rust using queue-known facts plus Rust sidecar membership, and public `insertTransaction` now uses one typed Rust runtime admission command that owns known-fast-path precheck, verification decisioning, latest FinalChain account sourcing, public status/message mapping, finalized-location mapping, queue mutation, and event/log intent instead of split C++ precheck/verify/admission sequencing. Rust legacy transaction envelope inspection now provides hashes, senders, nonces, gas fields, costs, intrinsic-gas coverage, signature validity, and canonical RLP payloads for `verifyTransaction`, validated admission, pack-session EVM inputs, DAG transaction persistence, finalized-status verification, non-finalized recovery, and proposal-period storage lookup identity checks. `saveTransactionsFromDagBlock` sends Rust-inspected transaction identities/RLP and senders to Rust; Rust sources latest sender account nonces from Rust FinalChain and owns sidecar membership checks, duplicate filtering, nonce-gated finalized-storage lookup, accepted ordering, target `TrxCount` planning, the accepted transaction storage batch, accepted non-finalized sidecar insertion, runtime count update, and accepted queue erasure before returning direct-hash queue-erased receipts for C++ logging. `getTransaction` keeps generic Rust storage lookup for single-object materialization; `getTransactions` and `getBlockTransactions` check the live C++ pool and Rust sidecar payloads first, then use a proposal-specific Rust storage lookup that resolves pending/finalized-regular/finalized-system/missing sources, verifies stored RLP hashes, inspects legacy sender/nonce identity in Rust, and filters old finalized storage hits with Rust FinalChain block-scoped account snapshots before C++ materializes accepted payloads. `recoverNonfinalizedTransactions` asks Rust storage for hash/RLP recovery entries and stale-finalized classification; Rust removes stale pending rows, validates survivor RLP hash and sender facts through the Rust envelope, and inserts survivor payloads into Rust runtime sidecars without returning count mirrors to C++. `updateFinalizedTransactionsStatus` sends finalized hashes/RLP payloads to Rust; Rust plans count increments, stale recently-finalized period eviction, current-period sidecar insertion, non-finalized sidecar removal, known-cache marking, queue erasure, runtime count update, and the legacy periodic queue purge while persisting `TrxCount` before returning direct-hash removed/queue-erased/purged receipts for C++ logging. Block-finalized queue expiry now calls the Rust runtime mutator directly; periodic finalized-account purge facts are sourced from the Rust FinalChain handle instead of C++ `FinalChain::getAccount`, and C++ no longer switches on raw lifecycle notice IDs for these mutation report consumers. The stale generic lifecycle/action report FFI surface has been removed in favor of operation-specific DAG-save, finalized-status, and admission reports; C++ no longer revalidates indexed mutation buckets, no longer mirrors transaction counts out of reports, and consumes direct hash receipts for remaining log/event sinks. `verifyTransactionsNotFinalized` now sends Rust-inspected transaction identities/senders to Rust, which sources latest account nonces before gated finalized-storage checks. C++ still owns locks, final transaction object materialization, event emission mechanics, logging, EVM gas-estimation execution, public transaction construction, and broader lifecycle/finalization orchestration. |
| Gas pricer | `transaction/gas_pricer.hpp`, `transaction/gas_pricer.cpp`, `gas_pricer_shim/*` | 171 lines | `rust-backed` | Rust domain/infra behind C++ overlay shim | Finalized-block history restoration, live finalized-block gas-price updates, minimum-price flooring, and percentile bid selection route through Rust under `RUSTAXA_ENABLE_GAS_PRICER`. |
| Pillar block/votes | `pillar_chain/pillar_block.hpp`, `pillar_chain/pillar_votes.hpp`, matching `.cpp` files, `pillar_votes_shim/*` | 627 lines | `rust-backed` for `PillarVotes` aggregation | Rust domain and overlay shim | `rustaxa-types::pillar` now mirrors `PillarBlock`, `ValidatorVoteCountChange`, `PillarVote`, `PillarBlockData`, optimized pillar-vote bundle, and current pillar data RLP/Solidity/hash shapes with malformed-input coverage and C++ golden-vector coverage for Solidity bytes. `PillarVote` now owns Rust author recovery for the legacy unsigned-hash/recoverable-signature contract and rejects out-of-range recovery IDs. `rustaxa-consensus::pillar_votes` owns already-verified vote uniqueness, weighted per-block aggregation, deterministic threshold subset selection, cleanup, Rust-backed vote inspection, and stateless PBFT sync bundle planning with stable rejection statuses and accepted vote weights. `RUSTAXA_ENABLE_PILLAR_VOTES` routes C++ `PillarVotes` through Rust while C++ keeps live `PillarVote` sidecars. Vote signing, manager orchestration, and JSON/RPC remain later slices. |
| Pillar manager | `pillar_chain/pillar_chain_manager.hpp`, `pillar_chain/pillar_chain_manager.cpp`, `pillar_chain_manager_shim/*` | 629 lines | `partial` | C++ orchestration initially, shim-owned Rust planners for narrow gates | Depends on FinalChain validator vote counts, network, storage, key manager. Do not introduce a full manager shim until FinalChain DPoS/state ports and manager orchestration boundaries are ready; current Rust-enabled builds already route the manager's in-memory `PillarVotes` member through the Rust-backed overlay. `isRelevantPillarVote` now uses a shim-owned Rust planner for period/block/known-vote relevance under `RUSTAXA_ENABLE_PILLAR_VOTES`, and `validatePillarVote` now inspects vote RLP/signature in Rust before using the Rust-recovered `(period, vote_hash, voter)` identity for uniqueness and DPoS eligibility. `addVerifiedPillarVote` in Rust mode also uses `pillar_vote_inspect`, recovered-voter weight lookup, and `addVerifiedVoteWithRecoveredVoter` so no `getVoterAddr`/legacy insertion path is re-run. A guarded `addPlannedVerifiedPillarVoteForRust` hook exists only so PBFT sync can mirror Rust-planned accepted votes and Rust-recovered voters into the live sidecar index without calling legacy manager validation, C++ voter recovery, or DPoS requery. Remove this hook when a full manager overlay owns planned insertion. |
| Rewards stats | `rewards/block_stats.*`, `rewards/rewards_stats.*` | 407 lines | `partial` | Rust deterministic domain plus shim-owned `Stats` routing | Rust-mode builds now route the `rewards::Stats` API surface through a shim overlay backed by the Rust rewards-stats runtime. The active Rust `FinalChain` native finalization path owns a long-lived Rust rewards-stats runtime, builds finalized-period facts with bridged previous-block cert votes, persists/reloads/clears interval cache rows in the finalized-block batch, and reuses the Rust planner for interval-boundary post-Magnolia DAG-author fee commission attribution. Native FinalChain now stages account mutation until finalization storage commits, credits supported post-Magnolia transaction-fee rewards to the Rust DPoS contract account while recording per-validator commission ownership in DPoS snapshots, and distributes supported fixed-yield plus Aspen part-two dynamic-yield minted block/DAG/vote rewards into validator commission plus delegator reward pools with header `total_reward` set from the Rust plan. Aspen total supply, yield, and read-only delegator reward pages are persisted in Rust DPoS snapshots and exposed through Rust-backed FinalChain reads. Rust now executes delegator reward claims and stake-mutation auto-claims against staged account/DPoS snapshots, supported native DPoS receipts carry legacy ABI event logs with header blooms derived in Rust, and Rust persists/routes the legacy two-level FinalChain log-bloom index for Rust-finalized blocks. C++ still owns legacy `BlockStats` data materialization for non-native reward distribution surfaces and broader final-chain rewards lifecycle orchestration. |
| Slashing manager | `slashing_manager/slashing_manager.*`, `slashing_manager_shim/*` | 102 lines | `partial` | Rust planner behind C++ overlay shim | Double-voting proof eligibility, canonical proof hash/cache, first funded submitter selection, contract address/gas/value envelope, and calldata construction route through Rust under `RUSTAXA_ENABLE_SLASHING_MANAGER`. `VoteManager::addVerifiedVote` now submits double-vote proofs from Rust-normalized unweighted vote payload records, so this route no longer needs a live C++ sidecar for the conflicting vote. C++ keeps FinalChain account reads, GasPricer bid, transaction signing, TransactionManager insertion, and the legacy live-vote overload for remaining compatibility callers. |
| Key manager | `key_manager/key_manager.*` | 55 lines | `cpp-owned` | C++ initially | Small wallet/secret wrapper; not on critical rewrite path. |

Current TransactionManager packing boundary: Rust now owns declared-gas and cached-gas handling inside the active
runtime pack session, so C++ receives a pack callback only when an EVM estimate is required. The stale standalone pack
planner FFI surface and explicit pack-finalize path have been removed; C++ cleans failed sessions through a Rust abort
entrypoint and still materializes only final selected RLP payloads. The shim-only public
`TransactionQueue::demoteToNonProposable` method has been removed because pack-session demotion now mutates the Rust
runtime queue directly.

Current TransactionManager read boundary: `getTransaction`, `getTransactions`, `getBlockTransactions`,
`getNonfinalizedTrx`, and `getPoolTransactions` consume Rust-owned transaction views that preserve request order and
duplicates while resolving queue, sidecar, pending-storage, finalized-regular, and finalized-system sources. C++ keeps
only transaction object materialization, logging, locks, EVM execution, event mechanics, and broader orchestration.

## Public API Tracker

### DAG

| Class | Public API groups | Dependencies | Tests | Target |
| --- | --- | --- | --- | --- |
| `Dag` / `PivotTree` | vertex/edge counts, `hasVertex`, `addVEEs`, leaves, ghost path, deterministic order, graph clearing | hashes, Boost graph today | `dag_test`, `full_node_test` ordering cases | Rust domain graph with byte/hash-compatible ordering |
| `DagManager` | block known/get/verify/add, pivot/tip availability, ordering, frontier, non-finalized blocks, anchors, expiry, VDF message | `DbStorage`, `TransactionManager`, `PbftChain`, `FinalChain`, `Network`, `KeyManager`, config | `dag_test`, `dag_block_test`, `pbft_manager_test`, `full_node_test` | C++ shim delegates pure graph/order logic to Rust first |
| `DagBlockProposer` | proposer lifecycle, propose block, select tips, proposer eligibility | `DagManager`, `TransactionManager`, `FinalChain`, `DbStorage`, `Network`, VDF | `dag_block_test`, `pbft_manager_test`, `sortition_test`, full-node tests | Rust-mode overlay keeps C++ thread/network shell; proposer eligibility status decisions, VRF input bytes, and deterministic tip selection are Rust-backed |
| `SortitionParamsManager` | params lookup, DAG efficiency, interval recalculation, cleanup | `DbStorage`, config, `PeriodData`, VDF params | `sortition_test`, `rust_consensus_tests`, `sortition_params_manager_shim_test`, full-node lambda tests | Rust deterministic calculations and runtime state; C++ storage/batch shell |

### PBFT

| Class | Public API groups | Dependencies | Tests | Target |
| --- | --- | --- | --- | --- |
| `PbftChain` | head/hash/size reads, block lookup, update head, block validation | `DbStorage`, `PbftBlock` | `pbft_chain_test`, `pbft_manager_test`, `full_node_test` | Early Rust-backed PBFT state slice |
| `ProposedBlocks` | push, mark valid, lookup, presence, cleanup, old-block checks | `DbStorage`, `PbftBlock` | `pbft_manager_test` proposed-block cases | Rust domain plus storage adapter |
| `PeriodDataQueue` | push/pop/clear/size/period/last block/old-data cleanup | `PeriodData`, `PbftVote`, peer `NodeID` | `rust_consensus_tests`, `period_data_queue_shim_test`, `pbft_manager_test`, full-node sync tests | Rust metadata queue plus C++ live payload shim |
| `PbftManager` | lifecycle, state machine, proposal generation, period/round/step, DPoS counts, sync queue, block validation, gossip, finalization, dynamic lambda | nearly every consensus subsystem | `pbft_manager_test`, `vote_test`, `pillar_chain_test`, `full_node_test`, Python integration | Split into Rust services after lower-level ports are stable |

### Votes and Eligibility

| Class | Public API groups | Dependencies | Tests | Target |
| --- | --- | --- | --- | --- |
| `VerifiedVotes` | vote insertion, unique voter tracking, step/round/period lookup, 2t+1 voted blocks, cleanup | `PbftVote` | `vote_test`, `pbft_manager_test` | Rust domain vote aggregation |
| `VoteManager` | vote validation, generation, reward votes, two_t_plus_one thresholds, VRF sortition, current period/round | `FinalChain`, `PbftChain`, `KeyManager`, `SlashingManager`, `DbStorage`, `Network`, VRF | `vote_test`, `pbft_manager_test` | Port validation/aggregation after DPoS ports exist; keep gossip shell in C++ |
| FinalChain DPoS ports | `dposIsEligible`, eligible vote count, total vote count, validators eligible vote counts, validators total stakes, total delegated, yield, total supply, VRF key, selected DPoS/slashing precompile reads | FinalChain/state API/EVM | `rust_consensus_tests`, `final_chain_test`, `rpc_test`, `pbft_manager_test`, `state_api_test`, proposer tests | Partial: genesis snapshot is Rust-backed and block numbers are preserved through the shim/bridge. Rust finalization now appends snapshots for native-transfer blocks, `registerValidator`, `delegate`, `undelegate`, `undelegateV2`, `confirmUndelegateV2`, `cancelUndelegateV2`, `reDelegate`, `claimRewards`, `claimCommissionRewards`, current-ABI `claimAllRewards`, legacy pre-fix `claimAllRewards(uint32)` batches, and slashing `commitDoubleVotingProof` jailing, records post-Magnolia transaction-fee commission rewards by finalized DAG block author through the Rust rewards-stat planner using bridged cert-vote facts, credits the supported fee-reward total to the Rust DPoS contract account, persists the DPoS/account snapshots, rewards-stat interval cache, and legacy FinalChain log-bloom index atomically with finalized-block visibility, and reloads those snapshots/cache rows on startup. Selected DPoS/slashing precompile reads use Rust snapshots so current validator commission rewards, delegator reward pages, V2 undelegation queues, jail blocks, and jailed validators are visible even when eligibility APIs use a delegation-delayed snapshot. Genesis delegations now cross the bridge into a Rust validator/delegator stake ledger so undelegation, redelegation, and slashing validator checks are Rust-owned. Rust-backed shim reads now serve total delegated, Aspen yield, Aspen total supply, `getTotalDelegation(address)`, `getDelegations(address,uint32)`, `getUndelegationsV2(address,uint32)`, `getUndelegationV2(address,address,uint64)`, slashing `getJailBlock`, slashing `getJailedValidators`, and `withBlockBloom` from Rust storage. Stake mutations with pending delegator rewards now auto-claim in Rust before changing stake, and receipts for the supported DPoS/slashing subset include Rust-generated `ValidatorRegistered`, `Delegated`, `Undelegated`, `UndelegatedV2`, `UndelegateConfirmedV2`, `UndelegateCanceledV2`, `Redelegated`, `RewardsClaimed`, `CommissionRewardsClaimed`, and `Jailed` logs in legacy order; claim-all gas is computed from the staged Rust DPoS delegation view. Missing historical snapshots, unsupported state/EVM transitions outside this subset, and Rust account-snapshot startup recovery still remain explicit gaps instead of falling back. |

### Transactions

| Class | Public API groups | Dependencies | Tests | Target |
| --- | --- | --- | --- | --- |
| `TransactionQueue` | insert/erase/get/order/group/contains/size/purge/known tx/min gas price | `FinalChain`, transactions | `transaction_test`, `transaction_queue_shim_test`, `gas_pricer_test`, `full_node_test` pool cases | Rust metadata and queued RLP payloads behind a full overlay shim; C++ materializes transactions and still owns known-cache timing plus FinalChain purge reads |
| `TransactionManager` | verify/insert/pack/get/finalize status/non-finalized recovery/gas estimation | `DbStorage`, `FinalChain`, thread pool, `DagBlock`, state API | `transaction_test`, `transaction_manager_shim_test`, `dag_block_test`, `pbft_manager_test`, `full_node_test` | Rust-backed packing, DAG persistence, finalized-status execution, storage-backed transaction lookup, Rust-owned transaction views for queue/sidecar/storage reads, non-finalized recovery reads, finalized filter/verification helpers, verification/validated-insert admission planning, Rust-owned queue payloads, typed Rust mutation command reports, and Rust-mode pending-transaction event emission with C++ materialization/estimation shell; continue moving lifecycle mutation before broader orchestration |
| `GasPricer` | gas price reads/calculation | `DbStorage`, `TransactionManager` | `transaction_test`, full-node transaction tests | Rust after queue semantics are stable |

### Pillar, Rewards, Slashing

| Class | Public API groups | Dependencies | Tests | Target |
| --- | --- | --- | --- | --- |
| `PillarBlock` / `PillarBlockData` | RLP, hash, JSON, Solidity encode/decode, validator vote-count deltas | hashes, state API data | `pillar_chain_test` encoding/finalization cases | Rust domain and codec parity |
| `PillarVotes` | vote uniqueness, threshold accumulation, above-threshold selection, cleanup | `PillarVote` | Rust `pillar_votes` unit tests; `pillar_votes_shim_test`; future `pillar_chain_test` parity | Rust-backed overlay under `RUSTAXA_ENABLE_PILLAR_VOTES`; C++ owns live vote sidecars and Rust owns deterministic aggregation |
| `PillarChainManager` | create block, validate/generate/finalize votes, current/finalized block state | `FinalChain`, `DbStorage`, `Network`, `KeyManager` | `pillar_chain_test`, `full_node_test` | Defer orchestration until FinalChain DPoS/state ports are real |
| `rewards::BlockStats` / `rewards::Stats` | per-block stats, interval recovery/processing/cleanup | `PeriodData`, PBFT votes, DPoS total vote count, storage | Rust `rewards_stats` unit tests, `rust_consensus_tests`, `rewards_stats_test`, full-node reward paths | partial: Rust deterministic runtime and CXX bridge planner produce legacy-compatible `BlockStats` RLP from finalized-period facts, append non-boundary cache writes to caller-owned Rust storage batches, and drive the `rewards::Stats` API surface through a full overlay under `RUSTAXA_ENABLE_REWARDS_STATS`. The active Rust `FinalChain` native finalization path now reuses the same Rust planner for interval-boundary fee commission attribution with persisted/reloaded cache rows and bridged cert-vote facts, then applies supported transaction-fee, minted reward, and delegator claim account/DPoS mutations to staged Rust snapshots, including Aspen part-two yield/supply state and supported DPoS receipt logs. C++ still owns legacy `BlockStats` objects for non-native `StateAPI::distribute_rewards` surfaces, log-bloom index routing, and final-chain orchestration outside the Rust native path. |
| `SlashingManager` | double-voting proof submission | `FinalChain`, `TransactionManager`, `GasPricer` | `slashing_manager_shim_test`, Rust slashing planner tests, limited existing indirect coverage | Rust deterministic planner plus Rust-normalized unweighted vote evidence payloads; C++ runtime shell until transaction construction/signing moves to Rust |

## First Slice: Rust DAG Graph

Status: `rust-backed` landed for C++ `Dag`/`PivotTree` graph operations under `RUSTAXA_ENABLE`. `DagManager`
orchestration remains C++, but Rust-enabled builds now keep a Rust-owned `DagManagerState` for deterministic in-memory
state. Frontier, ghost path, ordering, graphviz output, counters, anchors, period, expiry level, non-finalized indexes,
minimum difficulty, pivot/tip availability metadata, storage-backed persistence, and the first deterministic
`verifyBlock` reject decisions route through that state/runtime. The Rust-mode `DagManager` shim now owns the
`verifyBlock` flow directly for prechecks, Rust-planned transaction lookup, Rust-storage-backed missing transaction RLP
lookup for hashes not present in the live pool, DAG VDF payload/difficulty/proof verification, legacy DAG VRF/VDF
message construction, VDF/DPoS authorization decision ordering, and Rust-backed gas policy decisions instead of
forwarding the method wholesale to `DagManagerOld`.
Finalized DAG order application also now advances the
Rust `DagManagerState` directly, including empty-period advancement, and Rust-mode finalization cleanup asks Rust
storage for finalized-block counter facts, expired block hashes, and transaction cleanup hashes before Rust applies the
persistent cleanup. `DagManager::setDagBlockOrder()` now uses a single Rust apply call that resolves the anchor level
from Rust storage, computes finalization on a candidate state, applies finalized-block counter updates, expired DAG
deletes, and expired non-finalized transaction deletes in one Rust storage batch, commits Rust state, and returns only
the remaining shim-side cache/sidecar cleanup facts. Rust-mode non-finalized DAG sync reads the
Rust-owned period/index state rather than querying the old manager. Ordered non-finalized sync block selection now
returns a Rust-storage-backed payload with period, selected DAG block RLPs, and de-duplicated transaction RLP lookups;
C++ only reconstructs legacy `DagBlock` and `Transaction` objects for the public API. Non-finalized transaction query
planning and expired-block transaction cleanup selection also route through Rust hash plans while C++ still owns local
cache cleanup and live transaction-manager sidecar effects.

Target behavior:

- `Dag::hasVertex`
- Rust `DagGraph::add_vertex_edges` parity with C++ `Dag::addVEEs`
- leaf collection
- `PivotTree::getGhostPath`
- `Dag::computeOrder`
- vertex/edge counters and clear behavior

Rust design sketch:

- `rustaxa-consensus` has a `dag` module with explicit hash-keyed graph state instead of mirrored Boost graph types.
- Ordering is deterministic and covered by Rust unit tests and CXX bridge fixture tests.
- The bridge uses fixed hash bytes and explicit conversion at the boundary.
- `DagManager` remains in C++ during this slice; command-side DB writes, transaction handling, events, and networking
  still stay with the C++ side while deterministic in-memory state and storage-backed prechecks move through
  `DagManagerState`/`BridgeDagManagerRuntime` under `RUSTAXA_ENABLE`.

Required tests:

- Rust unit tests for graph insertion, leaves, reachability, ghost path, and deterministic order. Landed.
- CXX bridge fixture tests for Rust graph behavior. Landed under `rust_consensus_tests`.
- C++ public API regression/parity tests through the Rust-backed `Dag` wrapper. Landed through `dag_test`.
- Existing C++ tests: `dag_test`, `dag_block_test`, and ordering cases in `full_node_test`.
- Rust `verifyBlock` coverage for tip count/uniqueness, missing proposal-period mapping, expired block, transaction
  availability, VDF/DPoS authorization decision ordering, and gas-policy decisions. Landed in `rustaxa-consensus` and
  `rustaxa-bridge`; the shim now passes an explicit status-coded Rust VDF/DPoS fact envelope instead of encoding separate
  authorization branches in C++. DPoS/VRF facts are collected through a Rust FinalChain bridge bundle. Rust now decodes
  the DAG VDF payload, verifies the embedded VRF proof, calculates sortition difficulty, and verifies the Wesolowski
  proof against the exact legacy ASCII-hex modulus bytes used by C++ `VdfSortition`. Rust also builds the legacy
  `level + proposal-period-hash` VRF input, `pivot + transaction-hashes` VDF message bytes, and the verify-side VDF
  sortition denominator from Rust FinalChain config. The path no longer requires a `DagManagerOld::verifyBlock` method
  forward, and it no longer derives VRF output, VRF input, DAG VDF messages, or per-block verify-side VDF denominator
  policy through C++ consensus helpers. Producer-side `DagBlockProposer` now uses a full Rust-mode overlay shim for
  proposer eligibility status decisions, legacy VRF input construction, and deterministic tip selection, while C++ keeps
  the live thread/network shell, transaction packing, VDF compute wrapper, `DagBlock` construction/signing, and
  `DagManager::addDagBlock` wiring.

Open questions:

- Whether `computeOrder` must preserve every Boost traversal tie-breaker or only the externally visible block order.
- Whether direct C++ legacy `Dag` linking can be added to `rust_consensus_tests` without duplicate dependency symbols, or
  whether parity should stay fixture/transcript based.

## Validation Matrix

| Change area | Minimum validation |
| --- | --- |
| Rust consensus domain only | `cargo fmt --manifest-path rust/Cargo.toml`, `cargo clippy --manifest-path rust/Cargo.toml`, `cargo test --manifest-path rust/Cargo.toml` |
| DAG graph routing | Rust validation plus `rust_consensus_tests`, `dag_test`, `dag_block_test`, and `dag_shim_test` |
| DAG proposer routing | Rust validation plus `rust_consensus_tests`, `dag_block_test`, and proposer-path full-node or PBFT coverage when orchestration changes |
| Sortition params routing | Rust validation plus `rust_consensus_tests`, `sortition_test`, and `sortition_params_manager_shim_test` |
| PBFT chain/proposed-block/queue routing | Rust validation plus `rust_consensus_tests`, `pbft_chain_test`, `pbft_chain_shim_test`, `proposed_blocks_shim_test`, `period_data_queue_shim_test`, and relevant `pbft_manager_test` cases |
| Vote aggregation/eligibility | Rust validation plus `rust_consensus_tests`, `verified_votes_shim_test`, `vote_test`, relevant `pbft_manager_test`, and DPoS/state API coverage |
| Ingress message inspection/enrichment planning | Rust validation plus message-shape unit tests and C++ parity/golden-vector coverage for each routed ingress message kind; add scheduler or egress-event tests once the network pipeline lands |
| Transaction queue behavior | Rust validation plus `transaction_queue_shim_test`, queue-focused `transaction_test`, `gas_pricer_test`, and affected DAG/PBFT tests when manager/proposer packing changes |
| Slashing proof planning | Rust validation plus `slashing_manager_shim_test`; Rust byte-level proof-hash and calldata fixtures are required, with richer C++ legacy vote/submission transcripts still useful when available |
| Pillar vote aggregation and sync bundle validation | Rust validation plus `rust_consensus_tests` and `pillar_votes_shim_test`; broaden to `pbft_manager_test` and `pillar_chain_test` when manager behavior is touched |
| Pillar/reward behavior | Rust validation plus `pillar_chain_test`, `rewards_stats_test`, and affected full-node tests |
| PBFT manager state machine | Targeted PBFT/vote/DAG tests plus full-node smoke and Python integration coverage as needed |

## Current Open Items

| Item | Status | Owner decision needed |
| --- | --- | --- |
| Replace temporary DPoS query behavior | `partial` | Genesis DPoS vote-count, eligibility, validator total stake, validator eligible vote-count queries, total delegated, Aspen yield, Aspen total supply, `getValidator`, `getValidators`, `getValidatorsFor`, `getTotalDelegation`, `getDelegations`, V2 undelegation queue reads, `claimRewards`, `claimCommissionRewards`, current-ABI `claimAllRewards`, legacy pre-fix `claimAllRewards(uint32)`, slashing `getJailBlock`, slashing `getJailedValidators`, `withBlockBloom`, validator metadata/commission setters, Rust-executed double-vote jailing, and the `delegate`/`undelegate`/`undelegateV2`/`confirmUndelegateV2`/`cancelUndelegateV2`/`reDelegate` stake-mutation subset are Rust-backed for account/DPoS state. Rust-finalized native-transfer and supported DPoS/slashing mutation blocks now carry forward snapshots, post-Magnolia fee commission rewards, supported DPoS contract fee-balance credits, fixed/dynamic minted reward pools, direct delegator reward claims, commission reward claims, claim-all dynamic gas from staged DPoS state, owner-update and V2 undelegation failed receipts, auto-claim cursor updates, legacy ABI receipt logs for supported DPoS/slashing events, header blooms derived from those logs, legacy log-bloom index chunks, and restart-durable DPoS/account snapshot sidecars with a persisted validator/delegator stake ledger, validator insertion order, V2 undelegation queues with per-delegator IDs, last commission-change blocks, F1 reward cursors, Aspen supply state, slashing jail blocks, jailed-validator order, and duplicate double-vote proof keys. Remaining gaps: contract methods outside the supported registration/delegation/claim/owner-update/V2-undelegation/slashing subset and full failed-contract receipt parity for older supported mutation paths. |
| Create Rust DAG graph module | `rust-backed` | Landed as standalone Rust domain plus bridge tests and C++ `Dag` production routing through a full overlay shim under `RUSTAXA_ENABLE`. |
| Route sortition params through Rust | `rust-backed` | Landed under `RUSTAXA_ENABLE_SORTITION_PARAMS`; storage and write batches intentionally remain C++ owned for this slice. |
| Route verified votes through Rust | `rust-backed` | Landed under `RUSTAXA_ENABLE_VERIFIED_VOTES`; C++ shim preserves live `PbftVote` ownership while Rust owns deterministic index semantics and 2t+1 metadata. VoteManager Rust mode now consumes Rust-owned verified-vote state directly from the shim overlay for insertion, lookup, snapshots, cleanup, round advancement, and current-round 2t+1 bundle persistence. |
| Route DagManager verify/finalization state through Rust/shim | `partial` | Tip count/uniqueness, proposal-period availability, expiry, transaction availability, verify-side transaction query planning, verify-side missing transaction RLP lookup for hashes not present in the live pool, DAG embedded-VRF/VDF payload/difficulty/proof verification, legacy DAG VRF/VDF message construction, verify-side VDF max-vote ceiling selection, VDF/DPoS authorization ordering, gas-policy decisions, finalized DAG order application, empty-period advancement, expiry-level advancement, transitive expired-block classification, counter-update planning, storage-backed finalized counter facts, Rust-batched finalized counter writes, Rust-storage expired block removal, Rust-storage expired non-finalized transaction removal, non-finalized DAG sync period/index reads, ordered non-finalized sync block selection, Rust-storage-backed selected DAG block RLP loads, sync transaction RLP lookup, non-finalized transaction query planning, and expired-block transaction cleanup selection route through Rust. The shim still owns legacy block/transaction object materialization, live transaction-pool reads, gas estimation, local cache cleanup, and live transaction-manager sidecar effects, while DPoS/VRF facts now come from a Rust FinalChain bridge bundle and feed a single status-coded Rust envelope. Remaining gap: move broader orchestration and side effects out of the shim. |
| Route DagBlockProposer policy through Rust/shim | `partial` | Full overlay shim landed. Rust handles proposer eligibility status decisions, legacy VRF input construction, and deterministic tip selection. Transaction packing now routes through the Rust-owned TransactionManager pack session before proposer sharding. Remaining gaps: VDF compute payload assembly, block construction/signing, add-block wiring, and lifecycle/network orchestration still live in C++. |
| Route TransactionQueue metadata and payloads through Rust/shim | `rust-backed` | Full overlay shim landed under `RUSTAXA_ENABLE_TRANSACTION_QUEUE`. Rust owns deterministic queue metadata/order/limit behavior, canonical queued transaction RLP payloads, known-cache expiration, overflow/drop observation state, finalized-account purge planning, and latest FinalChain account-fact sourcing for both TransactionManager runtime cleanup and standalone `TransactionQueue::purge()`. C++ materializes `Transaction` objects from Rust-retained bytes. The old fact-supplied queue purge bridge APIs were removed after FinalChain-backed routing became authoritative. |
| Route TransactionManager packing, storage reads, and DAG transaction persistence through Rust/shim | `partial` | Rust runtime pack session landed for `packTrxs` candidate snapshotting, candidate scan, Rust-inspected candidate envelope facts for EVM input, declared-gas fit, invalid-estimate demotion mutation, accepted output ordering, accepted gas accumulation, and stop rules. C++ now drives `packTrxs` through a Rust step protocol that asks only for required EVM estimates, returns final selected RLP outputs, and clears the session without a C++ finalize step; a shim-owned guard serializes concurrent pack callers around the single Rust runtime session. The shim now owns a Rust `TransactionManagerRuntime` handle for live queue metadata/payloads, known-cache state, non-finalized and recently-finalized sidecars, authoritative transaction count, and bounded opaque gas-estimation cache state. `estimateTransactionGas` and `estimateTransactions` no longer forward to `TransactionManagerOld`; Rust plans declared-gas shortcuts, cache hits, cache misses, and cache insertion while C++ still executes FinalChain/EVM and decodes cached `ExecutionResult` RLP. Shared Rust legacy transaction envelope inspection now owns canonical RLP hashing, sender recovery/signature validity, system sender identity, nonce/gas/value/cost facts, and intrinsic-gas coverage for TransactionManager Rust-mode inputs. `saveTransactionsFromDagBlock` now sends Rust-inspected transaction identities/RLP and senders to Rust; Rust sources latest account nonces from Rust FinalChain, then performs sidecar membership checks, duplicate filtering, nonce-gated finalized-storage lookup, accepted ordering, count planning, accepted storage batching, accepted non-finalized sidecar insertion, accepted queue erasure, and direct-hash queue-erased receipt construction for C++ logging. `getTransaction`, `getNonfinalizedTrx`, and `getNonFinalizedTransaction` materialize Rust sidecar RLP hits before generic Rust storage misses are resolved with explicit source classification. `getTransactions` and `getBlockTransactions` now use a Rust proposal lookup for storage misses that resolves transaction payloads, verifies stored RLP hashes, inspects legacy transaction sender/nonce identity in Rust, and filters old finalized hits against Rust FinalChain block-scoped account snapshots before C++ materializes accepted payloads. `recoverNonfinalizedTransactions` now loads hash/RLP recovery entries through Rust, removes stale finalized pending rows in Rust storage, validates survivor payload hash/sender through Rust envelope inspection, and inserts survivor payloads into Rust runtime sidecars without returning count mirrors or lifecycle notices. `updateFinalizedTransactionsStatus` now sends finalized hash/RLP payloads to Rust; Rust plans count increments, stale recently-finalized eviction, current-period recently-finalized sidecar insertion, non-finalized sidecar removal, known-cache marking, queue erasure, typed finalized-status direct-hash receipt construction, and periodic finalized-account queue purge execution while persisting `TrxCount` before C++ logs side effects. Block-finalized queue expiry now calls the Rust runtime mutator directly, and periodic finalized-account purge reads sender account nonces from Rust FinalChain before mutating the Rust queue. `excludeFinalizedTransactions` and `verifyTransactionsNotFinalized` now use Rust-owned sidecar membership instead of C++ map facts; verify-not-finalized also sources latest sender account nonces from Rust FinalChain. `verifyTransaction`, `insertTransaction`, and `insertValidatedTransaction` now derive transaction envelope facts in Rust before entering typed Rust runtime admission command reports: Rust prechecks known state, performs public verification decisioning, sources latest account facts and finalized-location facts from Rust FinalChain, supports storage-completed admission, performs validated proposable/non-proposable queue mutation, returns event/log intent, and maps public statuses/messages. The stale generic lifecycle/action report bridge has been removed. The Rust-mode facade owns the public `transaction_added_` event surface and emits it from shim-owned code after Rust accepts a proposable admission. C++ still owns event dispatch mechanics, EVM gas-estimation execution, public transaction construction, final selected transaction materialization, and broader lifecycle/finalization orchestration. Remaining gap: move the remaining public construction, event/log mechanics, and broader manager orchestration before deleting larger C++ bodies. |
| Route SlashingManager proof planning through Rust/shim | `partial` | Rust planner landed under `RUSTAXA_ENABLE_SLASHING_MANAGER` for report flag gating, PBFT slot equality, canonical double-vote proof hash/cache, unweighted vote evidence payload normalization, submitter selection, contract envelope, and calldata construction. Rust now also verifies legacy PBFT double-vote calldata during FinalChain execution, recovers offending validators, applies Magnolia/Cacti jail times, emits `Jailed`, stores duplicate proof keys and jail state in Rust DPoS snapshots, and filters DPoS eligibility/total vote counts from that Rust jail state. C++ still owns live vote object selection, FinalChain account reads for proof submission, gas bid, transaction signing, and transaction-manager insertion; duplicate cache marking occurs only after successful transaction insertion. |
| Route PillarVotes aggregation through Rust/shim | `rust-backed` | Landed under `RUSTAXA_ENABLE_PILLAR_VOTES`; Rust owns period initialization, uniqueness, weighted block aggregation, threshold subset selection, cleanup, Rust-side pillar-vote inspection, and stateless PBFT sync bundle planning. C++ owns live `PillarVote` sidecars keyed by Rust-selected hashes and throws if a Rust-selected vote is missing locally. Direct `PillarVotesOld` vs Rust shim parity coverage now exercises period initialization, duplicate idempotence, same-voter conflict rejection, distinct-weight threshold selection, cleanup, and a planned-insertion path proving the Rust-recovered voter controls uniqueness instead of C++ sidecar recovery. The PBFT sync precheck now rejects period/block mismatches, failed C++ prevalidation facts, same-voter conflicts, zero weights, duplicate-only recounts below threshold, and weight overflow before planned insertion. Signing and broader pillar-chain finalization orchestration remain future work. |
| Route PBFT manager pillar-vote bundle validation through Rust | `partial` | The Rust-enabled sync path now lives in the full `PbftManager` overlay instead of an original-source early-return hook. The helper inspects each vote through Rust, uses Rust-returned period/block/vote hash/voter/signature status to build bundle facts, looks up DPoS weight in C++ with the Rust-recovered voter, calls the Rust bundle planner, returns explicit shim/planner statuses plus the first bad vote hash, and inserts only Rust-accepted votes with Rust-planned weights and Rust-recovered voter identities through a temporary PillarChainManager hook. This removes `validatePillarVote` / `addVerifiedPillarVote` revalidation, C++ signature recovery, C++ voter recovery, and DPoS relookup from insertion while keeping live sidecar insertion visible as temporary manager debt. Direct CXX bridge coverage for `pillar_vote_inspect` and `plan_pillar_vote_bundle` lives in `rust_consensus_tests`. Remaining PBFT manager state machine, proposal handling, finalization, and gossip orchestration are still C++ owned. |
| Route PBFT sync-period admission through Rust | `partial` | `rustaxa-consensus::pbft_sync` now owns the deterministic `processPeriodData` admission decision table for already-known and stale drops, previous-hash mismatch, missing/invalid FinalChain state, invalid reward/cert votes, invalid pillar data, and invalid required pillar votes. The CXX bridge uses stable u8 status codes plus transaction-hash wrappers, and the full `PbftManager` overlay passes facts at each existing sync checkpoint before applying Rust-returned side-effect intents. C++ still owns fact sourcing, logging, `waitForFinalized`, queue clearing, peer reporting, live `PeriodData`/vote object ownership, and final accept dispatch. Missing transaction and finalized-transaction facts are carried as warning signals and intentionally preserve legacy warn-only behavior. The overlay also guards empty cert-vote bundles before random vote selection so the Rust-planned rejection path cannot hit the old modulo-by-zero failure. |
| Route PBFT finalization persistence apply through Rust | `partial` | The Rust finalization bridge now owns the storage-batch lifecycle for PBFT finalized-period persistence stages. `apply_pbft_finalization_storage_writes` creates a Rust storage batch, appends ordered stage facts, commits on success, and drops the batch on rejected or failed stages while preserving the existing staged appender compatibility APIs. The PBFT manager overlay now calls this owned apply path for the primary finalized-period/reward-reset/sortition group, dynamic-lambda persistence, and post-FinalChain executed-status persistence. Rust also owns a finalization runtime session plus Cacti dynamic-lambda calculation: the bridge returns ordered mixed-executor actions, owns the action cursor, validates C++ action result reports before advancing, and returns block-period lambda, reward `blocks_per_year`, post-adjust rounds count, post-adjust dynamic lambda, and increase/decrease telemetry flags. The overlay consumes those Rust lambda outputs instead of calling the C++ `adjustDynamicLambda` routine in the Rust-mode finalization path. Rust now also classifies already-persisted PBFT blocks through a storage-backed resume inspector: durable hash-to-period, period-data, finalized DAG/transaction positions, optional period-lambda, executed-status, and FinalChain height facts produce explicit complete/replay-needed/missing-primary/conflicting-primary statuses. The PBFT overlay consumes replay-needed tail classifications through a Rust-owned resume runtime session: when primary finalization and dynamic lambda are already durable and FinalChain is exactly one period behind, it replays FinalChain finalization, persists executed status through Rust, sets the live executed flag, advances the PBFT period, and reports each action before Rust advances the cursor. Dynamic-lambda gaps, missing/conflicting primary facts, and complete duplicates remain explicit no-replay paths. DAG finalized-order mutation, transaction finalized-status sidecar cleanup, and PBFT-chain live head updates now report structured post-state proofs back to Rust before the normal finalization cursor advances; Rust validates finalized DAG counts, finalized transaction counts, finalized period, PBFT block hash, anchor hash, PBFT-chain size, and PBFT-chain head/anchor state against the accepted plan. VoteManager exposes a shim-owned reward-reset stage builder and only mutates live reward metadata after the Rust-owned batch commits. C++ still owns FinalChain dispatch mechanics, live field assignment from Rust plans, timers, logging, period advancement mechanics, sortition live replay, pillar post-processing, object materialization, and any restart replay of live side effects outside the bounded tail. |
| Route PBFT proposed-block restore and cleanup through Rust storage | `partial` | `ProposedBlocks` now exposes shim-owned storage-backed restore and cleanup operations. Rust iterates persisted proposed PBFT block RLPs, decodes each signed PBFT block link through `rustaxa-types`, validates the storage key against the decoded canonical block hash, and hydrates the Rust proposed-block index without materializing C++ `PbftBlock` objects during Rust-mode PBFT startup. Stale proposed-block cleanup now plans from the Rust index, deletes proposed-block storage keys in one Rust storage batch, and only then mutates the in-memory Rust index. The full `PbftManager` overlay calls the Rust-backed restore during startup while the original upstream-owned manager source remains clean. Live PBFT block object materialization, proposal handling, finalization, gossip, and vote sidecars remain C++ owned. |
| Route PillarChainManager relevance through Rust | `rust-backed` | The Rust-mode path now lives in the full `PillarChainManager` overlay instead of an original-source guarded hook. `isRelevantPillarVote` calls a shim-owned helper and stateless Rust relevance planner. Rust owns the deterministic period/block/known-vote decision and stable rejection statuses; C++ still owns current-block lookup, live vote sidecars, logging, network, storage, validation, signing, DPoS queries, and finalization. |
| Route PillarChainManager vote validation and block planning through Rust | `partial` | `validatePillarVote` inspects pillar-vote RLP/signature through Rust, uses the Rust-recovered `(period, vote_hash, voter)` identity for Rust-backed uniqueness checks, uses the recovered voter for C++ FinalChain DPoS eligibility, and returns explicit shim statuses for malformed votes, invalid signatures, conflicts, ineligible validators, and future DPoS state. `addVerifiedPillarVote` is routed the same way for insertion. `createPillarBlock` now asks Rust to plan validator vote-count deltas, and `isValidPillarBlock` asks Rust to validate first-block/period/parent-hash linkage before C++ materializes or persists pillar blocks. C++ still owns logging, FinalChain calls, bridge root/epoch facts, live sidecars, signing, network, storage, event emission, and finalization. |
| Route rewards stats planning through Rust | `partial` | `rustaxa-consensus::rewards_stats` now owns the deterministic `BlockStats` calculation for PBFT author, DAG block reward counts, fee rewards, cert-vote weights, max vote weight, interval caching, distribution boundaries, Magnolia fee gating, and Aspen DAG counting. `rustaxa-bridge` exposes a `BridgeRewardsStatsRuntime`, plain finalized-period facts, legacy-compatible `BlockStats` RLP output, and an appender for block-reward stats cache writes/clears on an existing Rust storage batch. Rust-enabled builds compile upstream `rewards::Stats` as `StatsOld` and expose a shim-owned `rewards::Stats` overlay that calls the Rust runtime for callers of that API surface. The active Rust `FinalChain` native finalization path now owns a long-lived rewards-stats runtime seeded from persisted `BlockRewardsStats`, passes bridged rewards config, DAG difficulty, `blocks_per_year`, transaction gas facts, finalized DAG transaction hashes, and previous-block cert-vote facts into the same Rust planner, stages cache writes/clears in the finalized-block batch before publishing `lastBlockNumber`, reloads cache rows after restart, and applies interval-boundary post-Magnolia transaction-fee plus fixed/dynamic minted rewards, delegator claims, supported DPoS receipt logs, and legacy log-bloom index updates to staged Rust account/DPoS snapshots and storage. Remaining gap: move legacy `BlockStats` carrier ownership fully into Rust. |
| Restore `pbft_manager.cpp` upstream shape after complete shim | `complete` | Rust-enabled builds now use a full `pbft_manager_shim` overlay: the overlay header remaps the upstream class to `PbftManagerOld`, the shim-owned class carries Rust-mode startup restore and sync-validation routing, and the original `libraries/core_libs/consensus/src/pbft/pbft_manager.cpp` is clean versus `upstream-main`. The remaining debt is overlay drift: upstream manager changes must be ported into `pbft_manager_overlay.cpp` until Rust owns the manager runtime. |
| Define consensus storage ports | `partial` | Started narrowly with `BridgeDagManagerRuntime` storage-backed DAG sync and finalization cleanup payload assembly: Rust selects non-finalized hashes, loads selected DAG block RLPs through Rust storage, decodes transaction references, performs shared Rust transaction RLP lookup, resolves finalized transaction status, returns finalized-block counter facts, and applies finalized counter updates plus expired DAG block and expired non-finalized transaction deletes through one Rust storage batch. Broader typed storage traits for consensus services are still needed before PBFT/DAG finalization side effects move fully into Rust. |
| Decide CXX bridge shape for consensus hashes and vectors | `rust-backed` for DAG graph | DAG bridge uses fixed bytes and explicit boundary conversion; revisit if PBFT/vote bridges need richer payloads. |
| Add C++/Rust DAG parity fixture | `rust-backed` | Rust bridge fixture tests and C++ public API regression tests landed. Direct in-process legacy-vs-Rust comparison remains optional if duplicate dependency symbols are resolved. |
| Vote packet duplicate-with-block delivery gap | `deferred` | Reproduced in `PbftManagerTest.propose_block_and_vote_broadcast`: some peers can miss proposed-block insertion when vote paths short-circuit in network packet handlers. Do not patch upstream-owned network C++ in this rewrite stream; track as network-module follow-up and resolve via rewrite-side network shim when network work starts. |
| Prepare consensus ingress for arena data pipeline | `partial` | Future network ingress will call `BridgeNetwork::ingest_network_packet(packet_type, from_node, data)` for latest-tarcap payload bytes, store accepted bytes in an application-owned arena, and communicate by ingress/enrichment payload reference ids after that. The returned `bool` is ingestion success only, not payload validity or consensus acceptance. Consensus slices should add raw-byte inspection and compact-fact planning surfaces for PBFT votes, DAG blocks, transactions, pillar votes, and PBFT sync data as those modules are touched. The first PBFT vote ingress planner now provides this shape for compact already-decoded vote facts and returns admit/drop/request-sync status without owning network packet bytes. The arena data plane is expected to expose multiple logical pipelines, not one monolithic consensus path: peer status/sync control, transaction admission, DAG admission, DAG sync, PBFT vote progress, PBFT sync/finalized-period intake, and pillar vote handling. The goal is to let prefilter and consensus stages drop or route events before expensive materialization, then emit typed pipeline/egress effects for the network module. Temporary guarded hooks in latest-tarcap vote packet handlers route Rust-enabled builds through this planner; those hooks should move into a network/tarcap pipeline overlay once that module exists. |
